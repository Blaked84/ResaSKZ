%h1 Listing typeresids

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @typeresids.each do |typeresid|
      %tr
        %td= link_to 'Show', typeresid
        %td= link_to 'Edit', edit_typeresid_path(typeresid)
        %td= link_to 'Destroy', typeresid, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Typeresid', new_typeresid_path
