%h1
  Evénements

%table.striped.alternate
  %thead
    %tr
      %th Evénement
      %th Description
    %tbody
      - @event.each do |e|
        %tr
          %td=link_to e.Name, eventshow_path(e.id)
          %td=e.Description