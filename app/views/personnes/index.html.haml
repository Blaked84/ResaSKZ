%table.striped.alternate
  %thead
    %tr
      %th Nom
      %th Prénom
      %th assurance
  %tbody
    - @personnes.each do |p|
      %tr
        %td=link_to p.nom, personne_path(p.id)
        %td=p.prenom
        %td=conditional_format(p.assurance)