<!DOCTYPE html>
<html>
<head>
	<%= stylesheet_link_tag    "application", media: "all"%>
	<%= javascript_include_tag "application"%>
	<%= csrf_meta_tags %>
	<%= javascript_include_tag "//www.google.com/jsapi", "chartkick" %>
	<%= include_gon %>
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title>Resa-SKZ</title>
</head>
<body>

	<%= render 'layouts/menu' %>


	<% flash.each do |key, value| %>
	<div id="notif" class="">
		<div class="<%= key %> notif"><%= value %></div>
	</div>
	<% end %>
<!-- 
	<%= javascript_include_tag "application" %>
	<%= custom_bootstrap_flash %>
 -->
 	<div id="top"></div>
	<div id="wrapper" class="mw960p center">
		<%= yield %>
	</div>

	<%if ENV['RAILS_ENV'] == "development"%>
	<div id="debug">ENV: <%= ENV['RAILS_ENV'] %><br/><%= debug(params) %><%= request.headers["X-Real-IP"] %></div>
	<%end%>



	<%if ENV['RAILS_ENV'] == "production"%>
	<script>
	(function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function()
	{ (i[r].q=i[r].q||[]).push(arguments)}

	,i[r].l=1*new Date();a=s.createElement(o),
	m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
	})(window,document,'script','//www.google-analytics.com/analytics.js','ga');

	ga('create', 'UA-1822825-35', 'auto');
	ga('send', 'pageview');

	</script>
	<%end%>

	<div class='footer center txtcenter'>
	157 & 84 me211 - <a href="http://skz.ueensam.org/" >skz.ueensam.org</a> - Contact : skz.ue@gadz.org
	</div>

</body>
</html>
